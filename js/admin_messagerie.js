// Admin messagerie page JS
// Initialize messaging-specific interactions
document.addEventListener('DOMContentLoaded', function() {
    if (typeof initMessaging === 'function') {
        initMessaging();
    }
});
