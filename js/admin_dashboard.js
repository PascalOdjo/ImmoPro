// Admin dashboard page JS
// Initializes dashboard-specific features.
document.addEventListener('DOMContentLoaded', function() {
    if (typeof initDashboardCharts === 'function') {
        initDashboardCharts();
    }
});
