// Admin bien-form page JS
// Handles photo preview and upload interactions for bien-form
document.addEventListener('DOMContentLoaded', function() {
    if (typeof initPhotoUploadPreview === 'function') {
        initPhotoUploadPreview();
    }
});
